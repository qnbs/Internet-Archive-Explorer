# Per-project pnpm configuration (non-workspace project).
# Overrides moved here from package.json because pnpm v10+ no longer reads
# the "pnpm" field in package.json.
overrides:
  protobufjs: '^7.6.3'
  ws: '^8.20.1'
  esbuild: '^0.28.1'
  '@babel/core': '^7.29.6'
