# ── Docker build context exclusions ──────────────────────────────────────────
# Keep the build context small and avoid leaking secrets / unnecessary files.

node_modules/
dist/
.git/
.github/
tests/
*.log
.env*
.vscode/
